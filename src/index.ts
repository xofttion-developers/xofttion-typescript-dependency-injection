import { InjectableFactory } from './factory';

export { Dependency } from './decorators/dependency.decorator';
export { Injectable } from './decorators/injectable.decorator';

export default InjectableFactory;
